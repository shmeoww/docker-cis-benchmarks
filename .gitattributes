# Хранить и выгружать все текстовые файлы с LF (Linux-окончания)
* text=auto eol=lf

# Явно бинарные файлы — не трогать
*.png binary
*.jpg binary
*.pdf binary
*.exe binary

# Windows-скрипты (если появятся) оставляем с CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf